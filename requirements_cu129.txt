# Backward-compatible aggregate requirements file.
# Windows users and existing automation may continue using this file.
# Linux/Runpod users should prefer scripts/setup_linux.sh so preflight checks,
# package indexes, model downloads, and runtime validation happen automatically.
-r requirements-base.txt
-r requirements-cuda-cu129.txt
torch==2.8.0+cu129
torchvision==0.23.0+cu129
torchaudio==2.8.0+cu129
